<%= provide(:body_id, 'index') %>
<%= provide(:result, '月报表日期') %>

<div class="container-fluid content-wrapper">
  <div class="row-fluid">
    <div class="span12">
      <div class="hero-unit">
        <button type="button" class="close hidden-phone" data-dismiss="alert">&times;</button>
        <h1>选择查询月份</h1>

        <p>
          本系统报表可以按次、日、周进行报表查看，可以指定日期进行下载相关报表，报表评估数据根据相关评分标准生成。
          如修改了相应的评分标准，不影响已生成的报表数据，只对往后的报表数据有影响，如对结果有异议，可申请核对。
        </p>
      </div>
    </div>
  </div>


  <p></p>
  <%= form_tag('month_r', :method => 'post', class: 'well form-horizontal') do %>
    <div class="control-group pull-left">
      <label class="control-label" for="month"></label>
    </div>
    <span class="add-on">选择月份</span>
    <%= select_tag :month, options_from_collection_for_select(@month, :first, :last), class: 'span1' -%>
    <%= submit_tag('查 询', class: 'btn btn-info span2 offset4') %>
  <% end %>
  <div class="row-fluid">
    <div class="span12">
      <h4 class="title">名言警句</h4>

      <div class="squiggly-border"></div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <blockquote>
        <p>
          Etiam varius erat in nisl egestas id auctor lacus consectetur.
          Ut dui risus, auctor a malesuada volutpat, tempus sit amet arcu.
        </p>
        <small class="pull-right">Jane Doe via Mobile</small>
      </blockquote>
    </div>
  </div>
</div>
<%= provide(:body_id, 'index') %>
<%= provide(:result, '查询负分原始记录') %>

<div class="container-fluid content-wrapper">
  <div class="row-fluid">
    <div class="span12">
      <div class="hero-unit">
        <button type="button" class="close hidden-phone" data-dismiss="alert">&times;</button>
        <h1>查询负分原始记录</h1>

        <p>
          负分原始记录可以按次统计周期查询来查看，如对结果有异议，可申请核对。
        </p>
        <br/>

        <p class="hidden-phone">
          <a class="btn btn-info btn-large pull-right">详细指南请猛击此处 &raquo;...</a>
        </p>
      </div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <div class="row-fluid hidden-desktop hidden-phone">
        <div class="span12">
          <h4 class="title">负分原始记录如下示：</h4>

          <div class="squiggly-border"></div>
          <table class="table table-striped table-bordered table-radmin">
            <thead>
            <tr>
              <th style="text-align:center;">测试时间</th>
              <th style="text-align:center;">出口名称</th>
              <th style="text-align:center;">测试网站</th>
              <th style="text-align:center;">首屏打开时间(ms)</th>
              <th style="text-align:center;">总时间(ms)</th>
              <th style="text-align:center;">吞吐率(KB/s)</th>
              <th style="text-align:center;">连接成功率(%)</th>
              <th style="text-align:center;">首页加载成功率(%)</th>
            </tr>
            </thead>
            <tbody>
            <% @out.each do |line| %>
              <% line.each do |row| %>
                <tr>
                  <% if row[0].blank? %>
                    <td style="text-align:center;"><%= row[0] %></td>
                  <% else %>
                    <td style="text-align:center;"><%= row[0].strftime('%Y-%m-%d %H:%M:%S') %></td>
                  <% end %>
                  <td style="text-align:center;"><%= row[1] %></td>
                  <td style="text-align:center;"><%= row[2] %></td>
                  <td style="text-align:center;"><%= row[3] %></td>
                  <td style="text-align:center;"><%= row[4] %></td>
                  <td style="text-align:center;"><%= row[5] %></td>
                  <td style="text-align:center;"><%= row[6] %></td>
                  <td style="text-align:center;"><%= row[7] %></td>
                </tr>
              <% end %>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <p></p>

  <div class="row-fluid">
    <div class="span12">
      <div class="well center">
        <div class="pagination pagination-small center offset4">
          <%= will_paginate @out, :inner_window => 2 %>
        </div>
        <div class="offset4">
          统计信息：一共有 <%= @out.total_entries %>  条,当前第 <%= @out.current_page %> 页, 共
          <% if (@out.total_entries % @out.per_page) ==0 %>
            <%= (@out.total_entries / @out.per_page) %> 页.
          <% else %>
            <%= (@out.total_entries / @out.per_page) +1 %> 页
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <h4 class="title">名言警句</h4>

      <div class="squiggly-border"></div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <blockquote>
        <p>
          Etiam varius erat in nisl egestas id auctor lacus consectetur.
          Ut dui risus, auctor a malesuada volutpat, tempus sit amet arcu.
        </p>
        <small class="pull-right">Jane Doe via Mobile</small>
      </blockquote>
    </div>
  </div>
</div>

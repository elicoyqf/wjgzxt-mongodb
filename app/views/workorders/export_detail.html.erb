<%= provide(:body_id, 'index') %>
<%= provide(:result, '工单查看') %>

<div class="container-fluid content-wrapper">
  <div class="row-fluid">
    <div class="span12">
      <div class="hero-unit">
        <button type="button" class="close hidden-phone" data-dismiss="alert">&times;</button>
        <h1>代理商所辖出口详细测试数据查看</h1>

        <p>
          以下列表根据出口对各个测试网站的评分进行统计，只显示测试数据总分为负值的数据条目。<br/>
        </p>
      </div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <ul class="breadcrumb">
        <li>
          <a href="/">
            <i class="radmin-icon radmin-home"></i>
            首页
          </a>
          <span class="divider">/</span>
        </li>
        <li>
          <a href="/workorders">
            <i class="radmin-icon radmin-file"></i>
            工单查看
          </a>
          <span class="divider">/</span>
        </li>
        <li>
          <a href="#">
            <i class="radmin-icon radmin-file"></i>
            选择日周月报表
          </a>
          <span class="divider">/</span>
        </li>
        <li class="active">
          <i class="radmin-icon radmin-file"></i>
          查看日周月报表
        </li>
      </ul>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <div class="row-fluid hidden-desktop hidden-phone">
        <div class="span12">
          <h4 class="title">[<%= @q_export %>] 出口测试数据如下示：</h4>

          <div class="squiggly-border"></div>
          <table class="table table-striped table-bordered table-radmin">
            <thead>
            <tr>
              <th style="text-align:center;">测试时间点</th>
              <th style="text-align:center;">目标网站</th>
              <th style="text-align:center;">目标网站URL</th>
              <th style="text-align:center;">目标IP地址</th>
              <th style="text-align:center;">目标归属地</th>
              <th style="text-align:center;">总负分值</th>
            </tr>
            </thead>
            <tbody>
            <% @q_data.each do |line| %>
              <tr>
                <td style="text-align:center;">
                  <%= line.test_time.strftime('%Y年-%m月%d日 %H:%M:%S') %>
                </td>
                <td style="text-align:center;"><%= line.dest_node_name %></td>
                <td style="text-align:center;"><%= line.dest_url %></td>
                <td style="text-align:center;"><%= line.dest_ip_address %></td>
                <td style="text-align:center;"><%= line.dest_locale %></td>
                <td style="text-align:center;"><%= line.negative_items_scores %></td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>

      </div>
    </div>
  </div>

  <p></p>

  <div class="row-fluid">
    <div class="span12">
      <div class="well center">
        <div class="pagination pagination-small center offset4">
          <%= will_paginate @q_data, :inner_window => 2 %>
        </div>
        <div class="offset4">
          统计信息：一共有 <%= @q_data.total_entries %>  条,当前第 <%= @q_data.current_page %> 页, 共
          <% if (@q_data.total_entries % @q_data.per_page) ==0 %>
            <%= (@q_data.total_entries / @q_data.per_page) %> 页.
          <% else %>
            <%= (@q_data.total_entries / @q_data.per_page) +1 %> 页
          <% end %>
        </div>

      </div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <h4 class="title">名言警句</h4>

      <div class="squiggly-border"></div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <blockquote>
        <p>
          Etiam varius erat in nisl egestas id auctor lacus consectetur.
          Ut dui risus, auctor a malesuada volutpat, tempus sit amet arcu.
        </p>
        <small class="pull-right">Jane Doe via Mobile</small>
      </blockquote>
    </div>
  </div>
</div>
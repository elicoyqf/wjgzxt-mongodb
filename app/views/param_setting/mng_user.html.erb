<%= provide(:body_id, 'param-setting') %>
<%= provide(:result, '系统说明') %>

<div class="container-fluid content-wrapper">
  <div class="row-fluid">
    <div class="span12">
      <div class="hero-unit">
        <button type="button" class="close hidden-phone" data-dismiss="alert">&times;</button>
        <h1>管理系统用户</h1>

        <div class="row-fluid hidden-desktop hidden-phone">
          <div class="span12">
            <p>
              先添加用户，然后再与出口进行关联，从而使之可以察看相关的工单报表。<br/>
            </p>
          </div>
        </div>
        <p></p>


        <p class="hidden-phone">
          <a class="btn btn-info btn-large pull-right">详细指南请猛击此处 &raquo;</a>
        </p>
      </div>
    </div>
  </div>

  <a href="adduser" class="btn btn-danger btn-large">添加新用户</a>
  <a href="interaction" class="btn btn-danger btn-large">关联出口</a>
  <a href="view_interaction" class="btn btn-info btn-large">查看关联信息</a>

  <p></p>

  <div class="row-fluid">
    <div class="span12">
      <div class="row-fluid hidden-desktop hidden-phone">
        <div class="span12">
          <h4 class="title">系统用户资料</h4>

          <div class="squiggly-border"></div>
          <table class="table table-striped table-bordered table-radmin">
            <thead>
            <tr style="text-align:center;">
              <th style="text-align:center;">登录名</th>
              <th style="text-align:center;">用户级别</th>
              <th style="text-align:center;">用户全名</th>
              <th style="text-align:center;">Email</th>
              <th style="text-align:center;">联系电话</th>
              <th style="text-align:center;">状态</th>
              <th style="text-align:center;" colspan="3">操作选项</th>
            </tr>
            </thead>
            <tbody>
            <% @user.each do |u| %>
              <tr>
                <td style="text-align:center;"><%= u.uname %></td>
                <td style="text-align:center;"><%= u.level %></td>
                <td style="text-align:center;"><%= u.alias %></td>
                <td style="text-align:center;"><%= u.email %></td>
                <td style="text-align:center;"><%= u.contact %></td>
                <td style="text-align:center;">
                  <% if u.status== 0 %>
                    正常
                  <% else %>
                    <span class="label label-warning">停用</span>
                  <% end %>
                </td>

                <td style="text-align:center;">
                  <% if u.uname == 'admin' %>
                    <a class="disabled btn btn-small btn-info" href="#">
                      <i class="icon-edit icon-white"></i>
                      更新
                    </a>
                  <% else %>
                    <a class="btn btn-small btn-info" href="u_user?id=<%= u.id %>">
                      <i class="icon-edit icon-white"></i>
                      更新
                    </a>
                  <% end %>
                </td>
                <td style="text-align:center;">
                  <% if u.uname == 'admin' %>
                    <a class="disabled btn btn-small btn-danger" href="#">
                      <i class="icon-edit icon-white"></i>
                      <% if u.status== 0 %>
                        停用
                      <% else %>
                        正常
                      <% end %>
                    </a>
                  <% else %>
                    <a class="btn btn-small btn-danger" href="s_user?id=<%= u.id %>">
                      <i class="icon-edit icon-white"></i>
                      <% if u.status== 0 %>
                        停用
                      <% else %>
                        正常
                      <% end %>
                    </a>
                  <% end %>
                </td>
                <td style="text-align:center;">
                  <% if u.uname == 'admin' %>
                    <a class="disabled btn btn-small btn-danger" href="#">
                      <i class="icon-trash icon-white"></i>
                      删除
                    </a>
                  <% else %>
                    <a class="btn btn-small btn-danger" href="d_user?id=<%= u.id %>">
                      <i class="icon-trash icon-white"></i>
                      删除
                    </a>
                  <% end %>
                </td>
              </tr>
            <% end %>

            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <h4 class="title">名言警句</h4>

      <div class="squiggly-border"></div>
    </div>
  </div>
  <div class="row-fluid">
    <div class="span12">
      <blockquote>
        <p>
          当一切似乎毫无希望时，我看着切石工人在他的石头上，敲击了上百次，而不见任何裂痕出现。
          但在第一百零一次时，石头被劈成两半。我体会到，并非那一击，而是前面的敲打使它裂开。
        </p>
        <small class="pull-right">贾柯·瑞斯</small>
      </blockquote>
    </div>
  </div>
</div>